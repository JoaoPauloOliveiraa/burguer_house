<p id="notice"><%= notice %></p>

<h1>Produtos</h1>

<table border="1px solid black">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Descricao</th>
      <th>Quantidade</th>
      <th>Tipo produto</th>
      <!--<th colspan="3"></th>-->
    </tr>
  </thead>

  <tbody>
    <% @produtos.each do |produto| %>
      <tr>
        <td><%= produto.nome %></td>
        <td><%= produto.descricao %></td>
        <td><%= produto.quantidade %></td>
        <td><%= produto.tipo_produto_id %></td>
        <td><%= link_to 'Listar', produto %></td>
        <td><%= link_to 'Editar', edit_produto_path(produto) %></td>
        <td><%= link_to 'Excluir', produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Produto', new_produto_path %>
