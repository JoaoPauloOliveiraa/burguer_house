<%= form_for @tipo_produto, :html => { :class => "form-horizontal tipo_produto" } do |f| %>

  <% if @tipo_produto.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tipo_produto.errors.count, "error") %> prohibited this tipo_produto from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tipo_produto.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nome, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nome, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nome) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit "Salvar", :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("Cancelar")),
                tipo_produtos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
